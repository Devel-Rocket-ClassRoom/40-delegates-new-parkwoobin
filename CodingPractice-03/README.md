# 코딩 연습 - 대리자 (3)

`Program.cs`에 아래 과제들의 코드를 순서대로 작성하세요.
모든 과제를 작성한 후 실행하면 각 과제의 실행 결과가 순서대로 출력됩니다.

> 💡 변수 이름이 겹치는 경우 중괄호 `{ }`로 코드를 감싸면 각 블록에서 같은 이름을 사용할 수 있습니다.

---

## 1. 내장 대리자(Action)로 멀티캐스트

"직진", "좌회전", "우회전"을 출력하는 정적 메서드 3개를 만드세요. `Action` 대리자에 `+=`로 모두 연결하고 호출하세요.

**실행 결과**

```
직진
좌회전
우회전
```

---

## 2. 익명 메서드와 멀티캐스트

`Action` 대리자에 위에서 만든 `GoForward`와 `GoLeft` 정적 메서드를 연결한 후, `delegate()` 키워드를 사용하여 "우회전"을 출력하는 익명 메서드를 `+=`로 추가하세요. 호출하여 결과를 확인하세요.

**실행 결과**

```
직진
좌회전
우회전
```
